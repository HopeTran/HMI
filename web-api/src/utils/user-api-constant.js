const PERMISSIONS = require('./permissions');

const USER_API_CONSTANT = {
  USER_API_JWT_EXPIRED_PERIOD: 10000 * 365 * 24 * 60 * 60 * 1000, // 10000 years
  API_PERMISSIONS: {
    
  },
};

module.exports = USER_API_CONSTANT;
